name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    name: PHP ${{ matrix.php }} Lint
    runs-on: ubuntu-latest
    strategy:
      matrix:
        php: ['8.0', '8.1', '8.2', '8.3']
    steps:
      - uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          tools: composer

      - name: Install dependencies
        run: composer install --no-progress --prefer-dist

      - name: Run PHPCS
        run: composer run phpcs

  build:
    name: Build ZIP
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4

      - name: Build plugin ZIP
        run: |
          mkdir -p build/images-sync-for-cloudflare
          cp -r plugin/* build/images-sync-for-cloudflare/
          cd build
          zip -r ../images-sync-for-cloudflare.zip images-sync-for-cloudflare -x '*.DS_Store' -x '*.git*'

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: images-sync-for-cloudflare
          path: images-sync-for-cloudflare.zip
          retention-days: 7
